# Promotable. CI bot in comp-proj-app-demo overwrites this on every
# main-branch build. Promotion path: dev → sit → uat → preprod → prod (each via PR).
- op: replace
  path: /spec/template/spec/containers/0/image
  value: public.ecr.aws/c5a3w2d1/backend:4913e49b74a6
