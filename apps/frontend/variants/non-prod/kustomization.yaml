apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Env-class label propagated to labels + selectors + pod templates.
# Strategy stays blue-green via base/rollout.yaml.

labels:
  - includeSelectors: true
    includeTemplates: true
    pairs:
      comp.io/env-class: non-prod
