# Env-bound. Never promoted — DB and Redis are env-specific by design.
database:
  url: postgresql://api-dev-db.comp.internal:5432/api_dev
  pool:
    max: 5

redis:
  url: redis://api-dev-redis.comp.internal:6379

region: ap-southeast-1
env: dev
