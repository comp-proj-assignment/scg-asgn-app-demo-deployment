# Promotable. Updated when a release branch is cut in comp-proj-app-demo.
# Same image then flows QA → STAGING → PROD by file-copy.
- op: replace
  path: /spec/template/spec/containers/0/image
  value: ghcr.io/comp/api:v3.3.0-rc1
